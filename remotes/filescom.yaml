name: Files.com
entryKey: obot-files-dot-com
shortDescription: Manage files, users, groups, permissions, and secure file sharing through Files.com's hosted MCP server
upgradeNote: |
  This replaces the Obot-hosted Files.com server with Files.com's official hosted MCP server.

  - **Reconnect required:** Provide your Files.com site subdomain and connect with either OAuth or a Files.com API key.
  - **OAuth setup:** A site administrator must enable MCP Dynamic Client Registration.
  - **Compatibility:** Custom domains are not accepted, tool parameters may change, and local file upload and download tools remain unavailable.
description: |
  Securely manage Files.com resources and perform file operations through [Files.com's hosted MCP server](https://www.files.com/docs/integrations/model-context-protocol-mcp-server), without running a local server.

  See Files.com's [official MCP server documentation](https://www.files.com/docs/integrations/model-context-protocol-mcp-server) for setup instructions, authentication options, and current capabilities.

  ## Features

  - **File & Folder Operations**: Find, copy, move, delete, list, zip, and unzip files and folders
  - **User and Group Management**: Create, update, list, and delete users and groups
  - **Share Link Management**: Create and manage secure file sharing links, recipients, registrations, and notifications
  - **Permission Management**: Manage folder-level permissions for users and groups
  - **History & Audit Logging**: Inspect file activity, logins, API requests, transfers, automations, and settings changes
  - **Automation and Remote Servers**: Inspect automation workflows and remote server connections

  ## What you'll need to connect

  Enter your Files.com site subdomain. For example, enter `acme` if your site is available at `https://acme.files.com`.

  Obot constructs the MCP endpoint as `https://<your-site>.files.com/api/ai/mcp/v1/`.

  Sign in with your Files.com account when prompted. OAuth authentication follows your existing Files.com user permissions and supports SSO. Your site's AI features must be enabled, and a Files.com Site Administrator must enable **MCP Dynamic Client Registration** under **AI > Settings** at `https://<your-site>.files.com/ui/aiFeatures/settings`.

  For automated or headless use, you can provide a Files.com API key instead of signing in. The key inherits the permissions of its associated Files.com user.

  Custom domains are not accepted; use the site's corresponding Files.com subdomain instead.

  > **⚠️ Upgrade notice**
  >
  > If you're upgrading this server, note that this replaces the Obot-hosted Files.com server with Files.com's official hosted MCP server.
  >
  > - **Reconnect required:** Provide your Files.com site subdomain and connect with either OAuth or a Files.com API key.
  > - **OAuth setup:** A site administrator must enable MCP Dynamic Client Registration.
  > - **Compatibility:** Custom domains are not accepted, tool parameters may change, and local file upload and download tools remain unavailable.
metadata:
  categories: Cloud & Infrastructure, File Transfer, Storage, Security & Compliance
  allow-multiple: "true"
  unsupportedTools: Download_File_to_Local,Upload_File_from_Local
icon: https://avatars.githubusercontent.com/u/59619169?s=48&v=4
repoURL: https://www.files.com/docs/integrations/model-context-protocol-mcp-server
config:
  - key: FILES_COM_SITE
    name: Files.com Site
    required: true
    sensitive: false
    description: Your Files.com site subdomain, for example `acme` for
      `https://acme.files.com`
    usage: env
  - name: Files.com API Key
    description: Optional Files.com API key for automated or headless authentication
    key: X-FilesAPI-Key
    required: false
    sensitive: true
    usage: header
runtime: remote
remoteConfig:
  urlTemplate: https://${FILES_COM_SITE}.files.com/api/ai/mcp/v1
toolPreview:
- name: List_Bundle
  description: List Share Links
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Find_Bundle
  description: Show Share Link
  params:
    id: Bundle ID.
- name: Create_Bundle
  description: Create Share Link
  params:
    description: Public description
    expires_at: Bundle expiration date/time
    max_uses: Maximum number of times bundle can be accessed
    note: Bundle internal note
    password: Password for this bundle.
    paths: A list of paths to include in this bundle.
    permissions: Permissions that apply to Folders in this Share Link.
    require_registration: Show a registration page that captures the downloader's
      name and email address?
- name: Update_Bundle
  description: Update Share Link
  params:
    expires_at: Bundle expiration date/time
    id: Bundle ID.
    permissions: Permissions that apply to Folders in this Share Link.
- name: Delete_Bundle
  description: Delete Share Link
  params:
    id: Bundle ID.
- name: List_Bundle_Download
  description: List Share Link Downloads
  params:
    bundle_id: Bundle ID
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: List_Bundle_Notification
  description: List Share Link Notifications
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Find_Bundle_Notification
  description: Show Share Link Notification
  params:
    id: Bundle Notification ID.
- name: Create_Bundle_Notification
  description: Create Share Link Notification
  params:
    bundle_id: Bundle ID to notify on
    notify_user_id: The id of the user to notify.
- name: Update_Bundle_Notification
  description: Update Share Link Notification
  params:
    id: Bundle Notification ID.
    notify_on_registration: Triggers bundle notification when a registration action
      occurs for it.
    notify_on_upload: Triggers bundle notification when a upload action occurs for
      it.
- name: Delete_Bundle_Notification
  description: Delete Share Link Notification
  params:
    id: Bundle Notification ID.
- name: List_Bundle_Recipient
  description: List Share Link Recipients
  params:
    bundle_id: List recipients for the bundle with this ID.
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Create_Bundle_Recipient
  description: Create Share Link Recipient
  params:
    bundle_id: Bundle to share.
    company: Company of recipient.
    name: Name of recipient.
    note: Note to include in email.
    recipient: Email addresses to share this bundle with.
- name: List_Bundle_Registration
  description: List Share Link Registrations
  params:
    bundle_id: ID of the associated Bundle
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Delete_File
  description: Delete File/Folder
  params:
    path: Path to operate on.
- name: Find_File
  description: Find File/Folder by Path
  params:
    path: Path to operate on.
- name: Zip_List_Contents_File
  description: List the contents of a ZIP file.
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    path: Path to operate on.
- name: Copy_File
  description: Copy File/Folder
  params:
    destination: Copy destination path.
    path: Path to operate on.
- name: Move_File
  description: Move File/Folder
  params:
    destination: Move destination path.
    path: Path to operate on.
- name: Unzip_File
  description: Extract a ZIP file to a destination folder.
  params:
    destination: Destination folder path for extracted files.
    path: ZIP file path to extract.
- name: Zip_File
  description: Create a ZIP from one or more paths and save it to a destination path.
  params:
    destination: Destination file path for the ZIP.
    paths: Paths to include in the ZIP.
- name: List_For_Folder
  description: List Folders by Path
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    path: Path to operate on.
- name: Create_Folder
  description: Create Folder
  params:
    path: Path to operate on.
- name: List_Group
  description: List Groups
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Find_Group
  description: Show Group
  params:
    id: Group ID.
- name: Create_Group
  description: Create Group
  params:
    admin_ids: A list of group admin user ids. If sent as a string, should be comma-delimited.
    name: Group name.
    notes: Group notes.
    user_ids: A list of user ids. If sent as a string, should be comma-delimited.
- name: Update_Group
  description: Update Group
  params:
    admin_ids: A list of group admin user ids. If sent as a string, should be comma-delimited.
    id: Group ID.
    name: Group name.
    notes: Group notes.
    user_ids: A list of user ids. If sent as a string, should be comma-delimited.
- name: Delete_Group
  description: Delete Group
  params:
    id: Group ID.
- name: List_User
  description: List Users
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Find_User
  description: Show User
  params:
    id: User ID.
- name: Create_User
  description: Create User
  params:
    authentication_method: How is this user authenticated?
    company: User's company
    email: User's email.
    group_ids: A list of group ids to associate this user with.  Comma delimited.
    name: User's full name
    notes: Any internal notes on the user
    password: User password.
    require_password_change: Is a password change required upon next user login?
    user_home: Home folder for FTP/SFTP.  Note that this is not used for API, Desktop,
      or Web interface.
    user_root: Root folder for FTP (and optionally SFTP if the appropriate site-wide
      setting is set).  Note that this is not used for API, Desktop, or Web interface.
    username: User's username
- name: Update_User
  description: Update User
  params:
    authentication_method: How is this user authenticated?
    company: User's company
    email: User's email.
    group_ids: A list of group ids to associate this user with.  Comma delimited.
    id: User ID.
    name: User's full name
    notes: Any internal notes on the user
    password: User password.
    require_password_change: Is a password change required upon next user login?
    user_home: Home folder for FTP/SFTP.  Note that this is not used for API, Desktop,
      or Web interface.
    user_root: Root folder for FTP (and optionally SFTP if the appropriate site-wide
      setting is set).  Note that this is not used for API, Desktop, or Web interface.
    username: User's username
- name: Delete_User
  description: Delete User
  params:
    id: User ID.
- name: List_Action_Log
  description: List Action Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `folder`, `path`, `src`, `destination`, `created_at`,
      `user_id` or `username`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `folder`, `path`, `src`, `destination`
      or `username`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Api_Request_Log
  description: List API Request Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `api_key_id`, `interface`, `request_id`, `request_ip`,
      `request_method`, `success`, `user_id` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `interface`, `request_id`, `request_ip`
      or `request_method`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Automation
  description: List Automations
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Find_Automation
  description: Show Automation
  params:
    id: Automation ID.
- name: List_Automation_Log
  description: List Automation Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `automation_id`, `automation_run_id`, `operation`, `path`,
      `status` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `path`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Email_Log
  description: List Email Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `status` and `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Exavault_Api_Request_Log
  description: List Exavault API Request Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `request_ip`, `request_method`, `success` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `request_ip` and `request_method`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_External_Event
  description: List External Events
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `created_at` and `status`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`, `status` or `event_type`.
- name: List_File_Migration_Log
  description: List File Migration Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `file_migration_id`, `operation`, `status`, `type` or
      `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Ftp_Action_Log
  description: List FTP Action Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `path`, `true_path`, `remote_ip`, `session_uuid`, `success`,
      `status`, `action_type`, `username` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `path`, `true_path`, `action_type` or `username`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_For_File_History
  description: List history for specific file.
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    display: Display format. Leave blank or set to `full` or `parent`.
    end_at: Leave blank or set to a date/time to filter later entries.
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    path: Path to operate on.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`.
    start_at: Leave blank or set to a date/time to filter earlier entries.
- name: List_For_Folder_History
  description: List history for specific folder.
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    display: Display format. Leave blank or set to `full` or `parent`.
    end_at: Leave blank or set to a date/time to filter later entries.
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    path: Path to operate on.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`.
    start_at: Leave blank or set to a date/time to filter earlier entries.
- name: List_For_User_History
  description: List history for specific user.
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    display: Display format. Leave blank or set to `full` or `parent`.
    end_at: Leave blank or set to a date/time to filter later entries.
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`.
    start_at: Leave blank or set to a date/time to filter earlier entries.
    user_id: User ID.
- name: List_Logins_History
  description: List site login history.
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    display: Display format. Leave blank or set to `full` or `parent`.
    end_at: Leave blank or set to a date/time to filter later entries.
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`.
    start_at: Leave blank or set to a date/time to filter earlier entries.
- name: List_History
  description: List site full action history.
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    display: Display format. Leave blank or set to `full` or `parent`.
    end_at: Leave blank or set to a date/time to filter later entries.
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `user_id`, `folder` or `path`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `path`.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`.
    start_at: Leave blank or set to a date/time to filter earlier entries.
- name: List_Inbound_S3_Log
  description: List Inbound S3 Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `operation`, `status`, `path`, `client_ip` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `operation`, `status`, `path` or `client_ip`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Outbound_Connection_Log
  description: List Outbound Connection Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `operation`, `status`, `src_remote_server_id`, `dest_remote_server_id`,
      `path`, `client_ip` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `path`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Permission
  description: List Permissions
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    group_id: ''
    path: Permission path.  If provided, will scope all permissions(including upward)
      to this path.
    user_id: ''
- name: Create_Permission
  description: Create Permission
  params:
    group_id: Group ID. Provide `group_name` or `group_id`
    path: Folder path
    permission: Permission type.  Can be `admin`, `full`, `readonly`, `writeonly`,
      `list`, or `history`
    user_id: User ID.  Provide `username` or `user_id`
- name: Delete_Permission
  description: Delete Permission
  params:
    id: Permission ID.
- name: List_Public_Hosting_Request_Log
  description: List Public Hosting Request Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `path`, `remote_ip`, `success` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `path`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Remote_Server
  description: List Remote Servers
  params:
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
- name: Find_Remote_Server
  description: Show Remote Server
  params:
    id: Remote Server ID.
- name: List_Scim_Log
  description: List Scim Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`.
- name: List_Settings_Change
  description: List Settings Changes
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `api_key_id` and `user_id`.
    per_page: 'Number of records to show per page.  (Max: 10000, 1,000 or less is
      recommended).'
    sort_by: If set, sort records by the specified field in either `asc` or `desc`
      direction. Valid fields are `created_at`, `api_key_id` or `user_id`.
- name: List_Sftp_Action_Log
  description: List SFTP Action Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `path`, `true_path`, `remote_ip`, `session_uuid`, `success`,
      `status`, `action_type`, `username` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `path`, `true_path`, `action_type` or `username`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Sync_Log
  description: List Sync Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `external_event_id`, `operation`, `status`, `sync_id`,
      `created_at` or `sync_run_id`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
- name: List_Web_Dav_Action_Log
  description: List WebDAV Action Logs
  params:
    cursor: 'Used for pagination.  When a list request has more records available,
      cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send
      one of those cursor value here to resume an existing list from the next available
      record.  Note: many of our SDKs have iterator methods that will automatically
      handle cursor-based pagination.'
    fields: Optional list of attribute names to include as columns in the response
      table. When omitted, a sensible default set is used. Useful for narrowing wide
      entities or surfacing fields not in the default.
    filter: If set, return records where the specified field is equal to the supplied
      value. Valid fields are `path`, `true_path`, `remote_ip`, `success`, `status`,
      `action_type`, `username` or `created_at`.
    filter_gt: If set, return records where the specified field is greater than the
      supplied value. Valid fields are `created_at`.
    filter_gteq: If set, return records where the specified field is greater than
      or equal the supplied value. Valid fields are `created_at`.
    filter_lt: If set, return records where the specified field is less than the supplied
      value. Valid fields are `created_at`.
    filter_lteq: If set, return records where the specified field is less than or
      equal the supplied value. Valid fields are `created_at`.
    filter_prefix: If set, return records where the specified field is prefixed by
      the supplied value. Valid fields are `path`, `true_path`, `action_type` or `username`.
    per_page: 'Number of records to show per page.  (Max: 2000, 1,000 or less is recommended).'
